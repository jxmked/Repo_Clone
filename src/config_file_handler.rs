pub struct Json_Handler {
  token: str,
  output_folder: str
}

mod json_handler {
  fn __load_json() {
    
  }

  pub fn get_token(&self) -> str {
    self.token;
  }

  pub fn get_output_folder(&self) -> str {
    self.output_folder;
  }

  pub fn set_token(&mut self, new_value: &str) {
    
  }
}